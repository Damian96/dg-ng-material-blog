import { FileRenderPipe } from './file-render.pipe';

describe('FileRenderPipe', () => {
  it('create an instance', () => {
    const pipe = new FileRenderPipe();
    expect(pipe).toBeTruthy();
  });
});
